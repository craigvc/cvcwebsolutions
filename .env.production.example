# Production Environment Variables Template
# Copy this to .env.production and fill in your production values

# Google Calendar API Configuration
GOOGLE_CLIENT_EMAIL=appointment-schedule@cvcwebsolutions.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
GOOGLE_CALENDAR_ID=craig@cvcwebsolutions.com

# Zoom API Configuration
ZOOM_ACCOUNT_ID=your_zoom_account_id
ZOOM_CLIENT_ID=your_zoom_client_id
ZOOM_CLIENT_SECRET=your_zoom_client_secret

# Zoom Webhook Configuration (optional)
ZOOM_WEBHOOK_SECRET_TOKEN=your_webhook_secret_token

# Payload CMS Configuration
PAYLOAD_SECRET=your-long-random-secret-key-change-this-in-production

# OpenAI API Key (for AI-powered content generation)
OPENAI_API_KEY=your_openai_api_key

# Application Settings
NODE_ENV=production
NEXT_PUBLIC_SITE_URL=https://cvcwebsolutions.com

# Database (SQLite - file-based, no configuration needed)
# Database file will be at: /app/data/payload.db

# Email notifications will be sent to the GOOGLE_CALENDAR_ID email
# Make sure the Google service account has Gmail API access

# IMPORTANT NOTES:
# 1. ADMIN_PASSWORD and SESSION_SECRET are NOT needed in production
#    (admin routes are blocked by middleware when NODE_ENV=production)
# 2. FOSSBilling variables removed as billing is not used for consultations
# 3. Sanity CMS variables removed as we're using Payload CMS only
